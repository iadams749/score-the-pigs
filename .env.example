# Copy to .env and fill in. .env is gitignored.

# Apple Developer Team ID, used for iOS native builds and signing.
# Find it at https://developer.apple.com/account under Membership details.
# Not needed for `npx expo start` / Expo Go — only for `npm run ios` and EAS.
APPLE_TEAM_ID=
